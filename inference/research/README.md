# Conan research-only inference utilities

This directory contains research and ablation tooling that is **not** part of the canonical Conan mainline surface.

Examples:

- factorized multi-reference swap builders
- factorized swap reports
- research JSON manifests for split-reference ablations

If you want the product/mainline path, use the files documented in `inference/README.md` instead.
